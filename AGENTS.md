# AGENTS.md

Правила этого репозитория. Установка, запуск, миграции и раскладка — в [README](README.md) и `docs/*.md`; здесь только то, чего в коде и конфигах не видно.

## Комментарий объясняет «почему»

Что делает код, видно из кода. В комментарий идёт то, чего не видно: какая альтернатива отвергнута и почему, какой сбой это предотвращает, что сломается, если сделать «очевидным» способом. Образцы — `src/security.ts` и `src/config/env.ts`: почти каждый комментарий там описывает поломку, которая уже случалась на боевом стенде.

Комментарии, сообщения коммитов и описания PR — на русском.

## Маркдаун: абзац — одна строка

Абзац в `.md` пишут одной строкой, разбивку по ширине делает рендер. При жёстких переносах правка одного слова перепаковывает весь абзац, и `git diff` показывает шум вместо изменения. Переносы в сообщениях коммитов остаются: их рендерит терминал, который не переносит сам.

Так написаны все `.md` репозитория, кроме двух. `docs/requirements.md` — сырой перенос заметок, где разрыв строки несёт смысл; `CHANGELOG.md` собирает release-please.

## Заголовок PR — это коммит в `main`

Мерж только squash (остальные стратегии отключены в настройках), поэтому заголовок PR остаётся в истории навсегда и по нему release-please считает версию и собирает CHANGELOG. Формат: `тип(подсистема): описание` — описание с маленькой буквы, без точки в конце.

Типов ровно шесть, они перечислены в `release-please-config.json`: `feat`, `fix`, `perf`, `revert`, `chore`, `build`. **Подсистема идёт в scope, а не в тип**: рефакторинг — `chore(runner):`, документация — `chore(docs):`, правка CI — `fix(ci):`. Заголовок с типом `docs:`, `ci:`, `refactor:`, `style:` или `test:` формально допустим по Conventional Commits, но проходит зелёным и молча выпадает из CHANGELOG и версии. Заголовки не проверяются (#951), так что это целиком на авторе.

Ветка — от `main`, имя вида `fix/database-ssl`. Коммиты внутри ветки в `main` не попадут: squash сворачивает их в заголовок PR.

Описание PR пишут для того, кто придёт читать код позже: какую проблему решали, какие варианты отвергли, что проверено и как.

## Перед пушем

```bash
npm run lint && npx tsc --noEmit && npx tsc --noEmit -p tsconfig.test.json && npm test
```

Проверок типов две: основная и `-p tsconfig.test.json`. Основной конфиг исключает `*.test.ts`, и ошибка, проходящая мимо первой проверки и валящая вторую, уже случалась (#945).

`npm test` — это vitest, один прогон на весь бэкенд. Нужна живая PostgreSQL: `docker compose up -d db`. Части набора запускаются так же, через `--`: `npm test -- src/runner` (тесты раннера, им не нужны ни Docker, ни база) и `npm test -- --exclude 'src/runner/**'` (всё остальное). Фронтенд проверяется отдельно, из `frontend/`: `npx tsc --noEmit && npm test`.

Зависимости ставятся через `npm ci`. Обычный `npm install` поверх существующего `node_modules` вычищает из `package-lock.json` платформенные пакеты — те, что помечены `os` и `cpu` и подключены через `optionalDependencies` ([npm/cli#4828](https://github.com/npm/cli/issues/4828), [npm/cli#7961](https://github.com/npm/cli/issues/7961)). В этом дереве их сорок один: 26 у esbuild (его тянут `drizzle-kit`, `tsx` и `vite`) и 15 у rolldown. Пропажа не видна локально — на своей платформе всё уже распаковано, и `npm ci` проходит, — но сломанный lockfile уезжает в коммит и роняет CI на linux. Проверять это надо на чистом дереве: `rm -rf node_modules && npm ci`.

Если записи уже пропали, вернуть их можно только полной пересборкой: `rm -rf node_modules package-lock.json && npm install`. Ни удаление одного `node_modules`, ни откат lockfile к прежней версии не помогают — npm считает lockfile источником истины и не добавляет то, чего в нём нет. Пересборка поднимает и остальные зависимости в пределах диапазонов, поэтому после неё обязательна проверка типов: так уже приезжал fastify, ломавший `tsc`.

`vite` лежит в `devDependencies`, хотя бэкенд ничего им не собирает, и удалять его как лишний нельзя. Vitest 5 держит vite в `peerDependencies`, а всё, что приходит по peer-пути, вычищается охотнее прочего — с явной зависимостью бинарники rolldown остаются на месте. На esbuild это не распространяется: он приходит другими путями, и его защищает только правило про `npm ci` выше.

Ветка обязана быть свежей относительно `main` — `git pull --rebase origin main`.

## После правки `src/` — перегенерировать типы для фронтенда

```bash
npm run generate:types
```

Фронтенд берёт тип роутера из `types/`. Без этого шага он проверяет типы против устаревшего слепка — и проверка проходит, потому что проверять нечего.

## После правки схемы — сгенерировать и закоммитить миграцию

```bash
npm run db:generate
```

Файл появляется в `drizzle/` и версионируется вместе с кодом; приложение применяет миграции само при старте. Почему генерировать их в сборке или в CI нельзя — в [README, «Миграции базы данных»](README.md#миграции-базы-данных).

## Выпуск — это мерж релизного PR

Теги руками не ставят, `make release` не существует. Версию считает release-please: он держит открытым PR `chore(main): release X.Y.Z`, и мерж этого PR и есть выпуск — тег, образы в GHCR, раскатка обоих стендов. Подробности — в [docs/deployment.md](docs/deployment.md).

## Куда смотреть перед правкой

| Правишь | Читай |
| --- | --- |
| вход, cookie, JWT, CSRF, политику паролей | [docs/auth.md](docs/auth.md) |
| исполнение кода, песочницу, языки раннера | [docs/runner.md](docs/runner.md) |
| контейнеры, стенды, переменные окружения, выпуск | [docs/deployment.md](docs/deployment.md) |
| персональные данные, согласие, 152-ФЗ | [docs/legal.md](docs/legal.md) |

## Agent skills

Сами скиллы лежат в `.agents/skills` (Claude Code видит их через симлинки в `.claude/skills`), обновляются целью `make skills-update`. Ниже — то, что скиллы обязаны знать про этот репозиторий.

### Трекер задач

Задачи и спецификации живут в GitHub Issues, операции — через `gh` CLI. Внешние PR потоком заявок не считаются. См. `docs/agents/issue-tracker.md`.

### Ярлыки разбора

Пять канонических ярлыков: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. См. `docs/agents/triage-labels.md`.

### Доменная документация

Один контекст: `CONTEXT.md` и `docs/adr/` в корне — их создаст `/domain-modeling`, когда появится что фиксировать. См. `docs/agents/domain.md`.
